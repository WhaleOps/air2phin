migration:
  # ---------------------------------------------
  # Different version of airflow import statement
  # ---------------------------------------------
  # airflow.operators.python_operator
  simple_python:
    src: |
      from airflow.operators.python_operator import PythonOperator
      
      def foo():
          print('Hello World!')
      
      bash = PythonOperator(
          task_id='python',
          python_callable=foo,
      )
    dest: |
      from pydolphinschsduler.tasks.python import Python
      
      def foo():
          print('Hello World!')
      
      bash = Python(
          name='python',
          definition=foo,
      )
  nested_python:
    src: |
      from airflow.operators.python_operator import PythonOperator
      
      def bar():
          print('Hello World!')
      
      def foo():
          bar()
      
      bash = PythonOperator(
          task_id='python',
          python_callable=foo,
      )
    dest: |
      from pydolphinschsduler.tasks.python import Python
      
      def bar():
          print('Hello World!')
      
      def foo():
          bar()
      
      bash = Python(
          name='python',
          definition=foo,
      )
